use util::type::*;
use util::error::*;
use util::config::SleepyConfig;
use bigint::hash::{H256, H520};
use bigint::uint::U256;

pub struct Sleepy {
    /// sleepy config
    config: SleepyConfig,
}

impl Sleepy {
    
}